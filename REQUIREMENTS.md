# ExcelSearch 产品需求

## 1. 产品目标

ExcelSearch 是一个在本机运行的跨平台 Excel 全文检索工具。

用户选择一个或多个 Excel 文件或文件夹后，程序读取工作簿 C 列的文字内容并建立本地索引。之后，用户只需输入记得的关键词，即可找到包含相关内容的 Excel 文件，以及内容所在的工作表和行号。

主要使用平台为 Windows，macOS 用于开发并作为完整支持的平台。

## 2. 核心使用流程

1. 用户添加 Excel 文件或选择一个包含 Excel 文件的文件夹。
2. 程序扫描文件，并以每个工作表的 C 列为主要内容在本地建立搜索索引。
3. 用户输入一个或多个关键词。
4. 程序显示所有匹配结果。
5. 用户查看命中的工作簿、工作表、单元格位置和内容摘要。
6. 用户可以打开原始 Excel 文件，并尽可能定位到对应工作表和单元格。

## 3. 第一版功能范围

### 3.1 文件导入

- 支持选择单个或多个 Excel 文件。
- 支持选择文件夹并递归扫描其中的 Excel 文件。
- 首批支持 `.xlsx`、`.xlsm` 和 `.xls`。
- 忽略 Excel 临时文件，例如以 `~$` 开头的文件。
- 同一个文件重复添加时不产生重复索引。
- 文件被移动、删除或无法读取时，在界面中明确显示状态。

### 3.2 内容索引

- 第一版默认只索引每个工作表 C 列中的非空文字内容；示例文件中该列为“物料名称”。
- C 列以外的单元格不参与全文匹配，避免其他编码或说明字段干扰搜索结果。
- A 列和 B 列可作为同一行的辅助信息保存和展示，例如“工厂代码”和“物料编号”，但默认不参与搜索。
- 记录文件路径、工作表名称、行号、C 列单元格坐标、C 列原始显示内容和标准化后的可搜索文本。
- 数字、日期和布尔值应转换为用户可理解、可搜索的文本。
- 公式单元格优先索引其已有计算结果；必要时也可索引公式文本。
- 不提取、不识别、不保存单元格中的图片，也不对图片进行 OCR；示例中的“图片”列应直接忽略。
- 索引保存在本机，不上传 Excel 内容。
- 通过文件修改时间和大小识别变化，只重新索引新增或发生变化的文件。
- 支持用户手动刷新全部索引或指定文件夹。
- 索引过程必须在后台运行，不能卡住搜索界面。

### 3.3 搜索

- 支持中文、英文、数字和中英文混合关键词。
- 默认不区分英文字母大小写。
- 支持内容中的部分匹配，不要求用户输入完整单元格内容。
- 多个关键词默认采用“全部包含”的匹配方式。
- 结果可以按相关程度排序，并允许按文件名或更新时间排序。
- 可按文件、文件夹、工作表名称和文件类型筛选结果。
- 搜索结果应去重并限制一次显示数量，避免大结果集卡顿。

### 3.4 搜索结果

每条结果至少显示：

- Excel 文件名。
- 文件完整路径。
- 工作表名称。
- C 列单元格坐标和行号，例如 `C27` / 第 27 行。
- 包含关键词的内容摘要，并高亮关键词。
- 同一行 A、B 列的辅助信息（存在时）。
- 文件最后修改时间。

支持以下操作：

- 打开文件。
- 在文件管理器中显示文件。
- 复制文件路径。
- 尽可能在 Excel 中打开并定位到匹配位置。

“定位到单元格”属于平台增强能力：Windows 可在安装 Excel 时使用可选的 Office 自动化；macOS 或未安装 Excel 时至少保证能够打开文件，并显示工作表和单元格位置供用户定位。

### 3.5 索引管理

- 显示已索引文件数量、单元格数量、索引更新时间和索引状态。
- 允许移除单个文件或整个文件夹的索引，但不删除原始 Excel 文件。
- 允许清空并重建索引。
- 清楚区分“从索引移除”和“删除磁盘文件”，第一版不提供删除原始文件的功能。

## 4. 非功能要求

- 所有 Excel 内容和索引默认只保存在本机。
- 无需联网即可完成导入、索引和搜索。
- Windows 为首要交付平台，同时支持 macOS 开发和使用。
- 正确处理包含空格、中文和其他非 ASCII 字符的文件路径。
- 单个损坏或受密码保护的文件不能导致整个索引任务失败，应跳过并报告原因。
- 搜索期间不修改原始 Excel 文件。
- 索引数据使用可重建的本地数据库；即使数据库损坏，也不能影响原始文件。

## 5. 默认技术方向

- 使用跨平台桌面界面。
- 使用本地 SQLite 数据库存储文件元数据和单元格索引。
- 针对中文和部分匹配采用适合中文的分词或 n-gram 索引方案，不能只依赖按空格分词。
- 文件读取、索引、搜索和平台集成功能相互分层，平台专属能力放在独立适配器中。
- 发布时生成 Windows 安装包，并提供 macOS 应用包用于验证和使用。
- Windows 最终用户无需安装 Python 或其他开发环境，直接通过安装包或便携版运行。

具体语言和 UI 框架应在开始实现时，以跨平台能力、打包稳定性、Excel 格式兼容性和后续维护成本为依据确定。

## 6. 第一版验收标准

第一版至少满足以下场景：

1. 添加一个包含多个 Excel 文件的文件夹后，可以完成索引。
2. 输入某个中文、英文或数字片段后，可以找到内容包含该片段的文件。
3. 搜索结果准确显示文件、工作表、单元格和内容摘要。
4. 修改某个 Excel 文件后刷新索引，搜索结果会反映新内容。
5. 可以从搜索结果打开原始文件或在文件管理器中显示它。
6. 损坏、加密或正在被占用的文件会显示错误，但不会影响其他文件。
7. 相同的核心功能在 Windows 和 macOS 上均可运行。
8. Windows 构建可生成 `ExcelSearch-Setup.exe` 安装包和解压即用的便携版。

## 7. 典型文件示例

对于当前提供的物料表样例：

- A 列是“工厂代码”。
- B 列是“物料编号”。
- C 列是“物料名称”，也是主要搜索内容。
- “图片”列及其中的产品图片全部忽略。

例如，搜索 C 列中的“四孔长方形”“0.9M”“TYPE-C”或“墨西哥”等文字片段时，结果应列出命中的 Excel 文件、工作表、C 列单元格和行号，并同时展示该行的工厂代码与物料编号，方便用户确认并打开正确的文件。

## 8. 暂不纳入第一版

- 云端同步或多人共享索引。
- 修改 Excel 内容。
- OCR 识别工作表中的图片。
- 索引或搜索图片列及其他嵌入图片。
- 搜索嵌入对象、图表内部文本或宏代码。
- 删除、移动或重命名原始 Excel 文件。
- 对加密 Excel 文件进行密码破解或绕过保护。
